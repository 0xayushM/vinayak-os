version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install -r vinayak/requirements.txt
run:
  command: uvicorn vinayak.api.main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
    env: PORT
  env:
    - name: PORT
      value: "8080"
